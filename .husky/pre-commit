#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔧 正在生成目录..."
npm run dir || exit 1  

# 把新生成的文档加入暂存区
git add doc/README.md

echo "✅ 文档生成并已添加到提交中"
