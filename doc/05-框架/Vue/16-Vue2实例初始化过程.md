# Vue2 实例初始化过程

```txt
new Vue() → _init()
    ↓
mergeOptions()
    ↓
initLifecycle()
initEvents()
initRender()
    ↓
beforeCreate()
    ↓
initInjections()
initState()
initProvide()
    ↓
created()
    ↓
$mount()
    ↓
compile template → render()
    ↓
mountComponent()
    ↓
beforeMount()
render()
update()
mounted()
```
